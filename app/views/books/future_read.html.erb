<%= provide(:title, "future reads") %>
<h2 class="text-2xl md:text-3xl font-custom text-coffee-default flex justify-center mb-3">to read in the near future:</h2>

<div class="grid justify-center">
  <div class="grid max-w-5xl md:grid-cols-5 gap-3 justify-items-center">
    <!-- cards --->
    <% @future_read_books.each do |book| %>
      <div class="max-w-xs shadow-lg rounded mt-3 px-1">
        <%= link_to image_tag(book_img(book.image, 'book_cover'), size: "180"), book_path(book) %>
      </div>
    <% end %>
  </div>
</div>