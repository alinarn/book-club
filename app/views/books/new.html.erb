<%= provide(:title, "add new book") %>
<h1 class="flex justify-center font-custom text-3xl text-coffee-default">Add New Book</h1>
<div class="flex justify-center">
  <%= render 'form', book: @book %>
</div>
